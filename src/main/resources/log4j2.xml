<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="logPath">./logs</Property>
        <Property name="archivePath">${logPath}/archive</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout disableAnsi="false"
                           pattern="%style{%d{HH:mm:ss}}{cyan} %highlight{[%-5level]}{FATAL=red blink, ERROR=red, WARN=yellow, INFO=green, DEBUG=blue} %style{[%t]}{dim} %style{%logger{1}}{magenta} - %msg%n" />
        </Console>

        <RollingFile name="File"
                     fileName="${logPath}/latest.log"
                     filePattern="${archivePath}/log-backup-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n" />
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
            </Policies>
            <DefaultRolloverStrategy max="10" />
        </RollingFile>
    </Appenders>

    <Loggers>
        <Logger name="net.dv8tion.jda" level="WARN" additivity="false">
            <AppenderRef ref="Console" />
        </Logger>

        <Root level="INFO">
            <AppenderRef ref="Console" />
            <AppenderRef ref="File" />
        </Root>
    </Loggers>
</Configuration>